import * as React from 'react';
import { View, Text } from 'react-native';
const NoResults = () => {
  return (
    <View>
        <Text style={{fontSize:20, color:'#888'}}>No Results Found</Text>
    </View>
  );
};

export default NoResults;